import React, { FunctionComponent } from 'react'
import { RouteComponentProps } from '@reach/router'

const Secrets: FunctionComponent<RouteComponentProps> = (
  props: RouteComponentProps
) => {
  return (
    <>
      <h1>Secrets</h1>
    </>
  )
}

export default Secrets
